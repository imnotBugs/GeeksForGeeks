class Solution {
  public:
    string armstrongNumber(int n) {
        // code here
        int a=n/100;
        int b=(n/10)%10;
        int c=n%10;
        if(a*a*a+b*b*b+c*c*c==n) return "true";
        else return "false";
    }
};
